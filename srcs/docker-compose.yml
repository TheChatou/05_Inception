# version: '2.33.1'

services:
  mariadb:
    image: mariadb
    build: ./builds/mariadb
    container_name: mariadb
    env_file: ./.env
    volumes:
      - mariadb_data:/var/lib/mysql
    networks: [inception]
    secrets:
      - db_root_pswd
      - db_pswd

  wordpress:
    image: wordpress
    build: ./builds/wordpress
    container_name: wordpress
    env_file: ./.env
    depends_on:
      - mariadb
    secrets:
      - db_pswd
    volumes:
      - wordpress_data:/var/www/html
    networks: [inception]

  nginx:
    image: nginx
    build: ./builds/nginx
    container_name: nginx
    depends_on: [wordpress]
    ports:
      - "443:443"
    volumes:
      - wordpress_data:/var/www/
    networks: [inception]

volumes:
  mariadb_data:
    name: mariadb_data
    driver: local
    driver_opts:
      type: none
      device: /home/fcoullou/data/mariadb
      o: bind
  wordpress_data:
    name: wordpress_data
    driver: local
    driver_opts:
      type: none
      device: /home/fcoullou/data/wordpress
      o: bind

networks:
  inception:
    name: inception
    driver: bridge

secrets:
  db_root_pswd:
    file: ../secrets/db_root_pswd.txt
  db_pswd:
    file: ../secrets/db_pswd.txt
